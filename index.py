per_total = 1
def sched(inputOne, inputTwo):
    x = clsblk(crse_name= inputOne, teach_name = inputTwo, period=per_total)
    append.schedulelist(x)








# The stuff belowdoesn't make sense because i don't know the button function in python
While True:
    onclick("add class"):
        per_total +=1
        sched()
